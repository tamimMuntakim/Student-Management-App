# frontend-react

This is a Vite + React frontend that mirrors the existing `frontend-web` static site but as a SPA.

Quick start:

1. cd frontend-react
2. npm install
3. npm run dev

The app expects the backend API at `http://localhost:8080/api` (same as the original frontend-web).
